/// <reference types="react" />
import User from '../UserType';
export declare type Props = {
    text?: string;
    image?: string;
    loading?: boolean;
    themeColor?: string;
    type?: "incoming" | "outgoing";
    user?: User;
    showAvatar?: boolean;
    showHeader?: boolean;
    last?: boolean;
    single?: boolean;
    clusterFirstMessage?: boolean;
    clusterLastMessage?: boolean;
};
export default function Message({ text, image, themeColor, loading, type, user, showAvatar, showHeader, last, single, clusterFirstMessage, clusterLastMessage }: Props): JSX.Element;
