/// <reference types="react" />
import User from '../../UserType';
declare type Props = {
    text?: string;
    image?: string;
    user?: User;
    themeColor?: string;
    showAvatar?: boolean;
    showHeader?: boolean;
    last?: boolean;
    single?: boolean;
};
export default function IncomingMessage({ text, image, user, showAvatar, showHeader, last, single, themeColor }: Props): JSX.Element;
export {};
