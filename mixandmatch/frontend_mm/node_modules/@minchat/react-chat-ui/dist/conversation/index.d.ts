/// <reference types="react" />
import MessageType from '../MessageType';
export declare type Props = {
    title: string;
    lastMessage?: MessageType;
    avatar?: string;
    onClick: () => void;
    selected?: boolean;
    themeColor?: string;
    /**
     * the current user on the chat ui
     */
    currentUserId?: string;
};
export default function Conversation({ title, lastMessage, onClick, avatar, selected, themeColor, currentUserId }: Props): JSX.Element;
