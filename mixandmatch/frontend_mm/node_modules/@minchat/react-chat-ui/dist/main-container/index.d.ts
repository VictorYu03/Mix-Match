/// <reference types="react" />
import { Props as MessageContainerProps } from '../message-container';
import { Props as ConversationContainerProps } from '../conversation-container';
import "../index.css";
export interface Props {
    mobileView?: boolean;
    inbox: ConversationContainerProps;
    selectedConversation?: MessageContainerProps;
}
/**
 *
 * a chat view that automatically switches between desktop view and mobile view
 * @returns
 */
export default function MainContainer({ mobileView, inbox, selectedConversation }: Props): JSX.Element;
