/// <reference types="react" />
export declare type Props = {
    onBack?: () => void;
    children?: string;
    showBack?: boolean;
    mobileView?: boolean;
};
export default function MessageHeader({ onBack, children, showBack, mobileView }: Props): JSX.Element;
